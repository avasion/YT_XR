# Copy this file to ".env" and paste your real key after the "=".
# ".env" is gitignored, so your key never gets committed.
#
# For the LIVE site, you do NOT use this file — you set YOUTUBE_API_KEY
# in Netlify (Site settings -> Environment variables) instead, so the key
# never touches your repo or the browser.

YOUTUBE_API_KEY=
